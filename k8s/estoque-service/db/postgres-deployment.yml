apiVersion: apps/v1
kind: Deployment
metadata:
  name: estoque-db
  labels:
    app: estoque-db
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: estoque-db
  template:
    metadata:
      labels:
        app: estoque-db
    spec:
      containers:
        - name: estoque-db
          image: postgres:16
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: estoque-db-secret
          volumeMounts:
            - name: estoque-db-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "estoque"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "postgres", "-d", "estoque"]
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
      volumes:
        - name: estoque-db-data
          persistentVolumeClaim:
            claimName: estoque-db-pvc
