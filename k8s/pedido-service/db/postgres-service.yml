apiVersion: v1
kind: Service
metadata:
  name: pedido-db
  labels:
    app: pedido-db
spec:
  selector:
    app: pedido-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  type: ClusterIP